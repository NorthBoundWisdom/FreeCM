## 当前仓库本身可以提升的点

### 进度说明

- [ ] 未完成
- [x] 已完成
- 进行中的事项先保持未勾选，并在条目文字里标注“进行中”。

### Checklist

- [ ] 1. 拆分过大的 `freecm/dependency_roots.py`（进行中）
  - [x] 保留一个对外兼容的 Facade 类。
  - [x] 拆出 `lock_manager.py`。
  - [x] 拆出 `closure_resolver.py`。
  - [x] 拆出 `seed_store.py`。
  - [x] 拆出 `materializer.py`。
  - [x] 拆出 `conflict_resolver.py`。
  - [x] 拆出 `dependency_cli.py`。
  - [x] 保持 C++、Swift、Android、.NET 适配层的现有导入和行为兼容。

- [x] 2. 拆分过大的 `vscode-extension/src/extension.ts`
  - [x] 拆出 workflow controller。
  - [x] 拆出 lock mode controller。
  - [x] 拆出 repo command controller。
  - [x] 拆出 webview state builder。
  - [x] 拆出 workspace discovery adapter。
  - [x] 拆出 terminal/session manager。
  - [x] 保持现有 VS Code extension 命令、状态和测试行为兼容。

- [x] 3. 增加更明确的架构图和面试说明文档
  - [x] 新增 `docs/architecture.md`。
  - [x] 说明 `source_roots.lock.jsonc.in` 到 `dependency_seed_repos` 的 `--init` 网络流程。
  - [x] 说明 `dependency_seed_repos` 到 `dependency_source_roots` 的 `--update` 离线流程。
  - [x] 说明 adapter 如何连接 CMake、Xcode、VS Code commands。
  - [x] 解释 A/B/C/D 依赖闭包和 ABI 风险模型。

- [x] 4. 清理 `.codex/freecm-wiring/SKILL.md` 里的旧字段痕迹
  - [x] 删除 `abiGroup` 作为 dependency entry 字段的示例或说明。
  - [x] 确认该 skill 与 `dependency_lock.py` 和正式 schema 文档一致。

- [ ] 5. 降低测试输出噪声并改善本地测试耗时
  - [ ] 给 git helper 增加测试静默模式。
  - [ ] 将重型 integration tests 和轻量 unit tests 分层。
  - [ ] 复用 fixture 或减少重复 clone。
  - [ ] 保持 CI 全量验证。
  - [ ] 提供本地 `fast` test profile。

- [ ] 6. 将 lock 文件写入改成 atomic write
  - [x] 找出 active lock/template 的直接 `write_text` 写入点。
  - [x] 写入临时文件。
  - [x] 对临时文件执行 fsync。
  - [x] 用 rename/replace 原子替换目标文件。
  - [x] 增加简单 file lock，避免多个窗口同时 Pin/Update 导致半写入或互相覆盖。
  - [ ] 增加覆盖 VS Code 插件、CLI、并发操作风险的测试。

- [ ] 7. 增强 policy 能力
  - [ ] 增加 remote URL normalize，例如 SSH/HTTPS 同仓等价识别。
  - [ ] 评估并预留 commit/tag 签名验证接口。
  - [ ] 评估并预留 allowed branch/ref 策略。
  - [ ] 评估并预留 SBOM/许可证扫描接口。
  - [ ] 评估并预留 dependency owner approval hook。
  - [ ] 评估并预留 vulnerability report 接入点。
  - [ ] 在文档中明确当前 policy 是“可接入组织治理的基础对象”，不是完整供应链安全平台。

- [ ] 8. 评估 CMake dependency build spec 的 self-describing metadata
  - [ ] 保留父仓显式配置的当前默认设计。
  - [ ] 评估依赖声明最小 build metadata 的边界，例如是否 install、默认 options、required packages。
  - [ ] 避免重新引入“子仓自己 bootstrap 自己依赖”的问题。
  - [ ] 将可落地规则写入合适文档，避免停留在规划说明里。

- [ ] 9. 增加最小示例仓库或 e2e fixture
  - [ ] 设计 `examples/abc-chain/` 或独立 demo。
  - [ ] 覆盖 `AppA`。
  - [ ] 覆盖 `LibB -> LibC, LibD`。
  - [ ] 覆盖 `LibC -> LibD`。
  - [ ] 覆盖 `LibD`。
  - [ ] 提供一条命令演示 `--init`。
  - [ ] 提供一条命令演示 `--update`。
  - [ ] 提供一条命令演示 `graph --format dot`。
  - [ ] 提供 CMake 构建顺序演示。

- [ ] 10. 显式报告 root lock 覆盖 transitive pin 的 commit 不一致
  - [ ] 当大仓 A 的 root lock 直接声明 D，并覆盖 B/C 嵌套 lock 中的 D commit 时，在 `audit` 或 `resolve` 中报告 `root override transitive pin mismatch`。
  - [ ] 默认不一定失败，但必须让诊断可见，避免 B 要求 D@commit1、A 统一到 D@commit3 后，兼容性问题只能到编译或测试阶段才暴露。
  - [ ] 报告中包含被覆盖依赖名、root commit、transitive commit、声明来源和父依赖名。
  - [ ] 报告commit sha要用颜色高亮显示，例如红色表示不一致，绿色表示一致。
